---
title: ディレクトリ内のすべてのEmacs Lispファイルを一発でバイトコンパイル
tags:
  - Emacs
  - emacs-lisp
private: false
updated_at: '2025-12-04T19:13:39+09:00'
id: 26cec55dc50b1af25297
organization_url_name: null
slide: false
ignorePublish: false
---
## はじめに
ディレクトリ内のEmacs Lispファイルを一発でバイトコンパイルするには,
Emacsから下記を実行すれば良い.

> C-u 0 M-x byte-recompile-directory

しかし, 再帰的には適用されないため,
第2階層以降のディレクトリ内にあるファイルはバイトコンパイルできない.

## 対応策

そこで, Emacs内からでなく, 下記のようにコマンドラインからバイトコンパイルできることを利用する.

```console
$ emacs -batch -f batch-byte-compile /path/to/*.el
```

したがって, 下記のようにすれば, ディレクトリ内のファイルを再帰的にバイトコンパイルできる.

```console
$ find /path/to -name '*.el' | xargs emacs -batch -f batch-byte-compile
```

なお, Zshでは簡潔に, 下記でバイトコンパイルできる.

```console
$ emacs -batch -f batch-byte-compile /path/to/**/*.el
```

## 参考
https://www.gnu.org/software/emacs/manual/html_node/elisp/Compilation-Functions.html
