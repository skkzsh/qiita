---
title: magit v4のel-getでエラー
tags:
  - Emacs
  - magit
  - el-get
private: false
updated_at: '2025-12-04T20:54:41+09:00'
id: 40dc5300c3dd03f291d3
organization_url_name: null
slide: false
ignorePublish: false
---
## magit v4のel-getでエラー

https://magit.vc/

magitのel-getは以下のように設定している.

```emacs-lisp:~/.emacs.d/init.el
(el-get-bundle magit)
```

しかしmagitのv4系から, 以下のようなエラーが出るようになった.

```console
Generating magit.html
Generating magit-section.html
Generating magit/*.html
Generating magit-section/*.html
Generating magit.pdf
You don't have a working TeX binary (tex) installed anywhere in
your PATH, and texi2dvi cannot proceed without one.  If you want to use
this script, you'll need to install TeX (if you don't have it) or change
your PATH or TEX environment variable (if you do).  See the --help
output for more details.

For information about obtaining TeX, please see http://tug.org/texlive,
or do a web search for TeX and your operating system or distro.
make[1]: *** [magit.pdf] Error 1
make: *** [docs] Error 2
```

PDFの生成時にTeXがなくてエラーとなったようである.

## 対処方法

そもそもPDFの生成が不要.

そのため`make`の[デフォルトターゲット](https://github.com/magit/magit/blob/v4.0.0/Makefile#L13
)から[`docs`](https://github.com/magit/magit/blob/v4.0.0/docs/Makefile#L8)を除外して, ターゲットは[`lisp`](https://github.com/magit/magit/blob/v4.0.0/Makefile#L76)や[`texi`](https://github.com/magit/magit/blob/v4.0.0/docs/Makefile#L10), [`info`](https://github.com/magit/magit/blob/v4.0.0/docs/Makefile#L11)あたりまでを指定するように`:build`オプションで上書きする.

```emacs-lisp:~/.emacs.d/init.el
(el-get-bundle magit
  :build '(("make" "clean" "lisp" "texi" "info"))
  )
```

## バージョン情報
```emacs-lisp:*scratch*
(magit-version)
"v4.4.2-75-g3fe795e5-dirty"
```
